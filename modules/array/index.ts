import transpose from './transpose';
import getShape from './getShape';
import dot from './dot';
import exp from './exp';
import {
  sum, minus, multiply, divide,
} from './calc';

export default {
  sum,
  minus,
  multiply,
  divide,
  getShape,
  transpose,
  dot,
  exp,
};
